# Gateway Timeout Register

## Historical Setting

VX Gateway used timeout 800 ms in release REL-2025.09. That value is retained only for audit history.

## Current Setting

VX Gateway uses timeout 1200 ms in release REL-2026.02. The current setting applies to production traffic.
