<!-- benchmarks/corpora/holdout2/code/README.md -->
# Holdout2 Code Notes

The legacy access guide says guest collaborators can export Amber project data
and archived accounts remain export eligible. This note is stale.

The legacy incident guide says major incidents are never declared before 60
minutes. The implementation has a lower duration threshold and a customer-count
threshold.

The legacy pricing guide says discount overrides are not controlled by
environment variables. The implementation contains the current override rule.
